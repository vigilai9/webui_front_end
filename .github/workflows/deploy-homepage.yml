name: Deploy to cPanel

on:
  push:
    branches: [ main ] # or your production branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18' # or your preferred version
    
    - name: Install dependencies
      run: npm install # or yarn install
    
    - name: Build project
      run: npm run build # or your build command
      env:
        NODE_ENV: production
    
    - name: Zip build output
      run: |
        cd out # or your build output directory
        zip -r ../out.zip .
  
    - name: Delete files in public_html
      uses: appleboy/ssh-action@master
      with:
       host: 's3cura.ai'
       username: 'iqfu27x9dob7'
       password: 'Gaurav123#'
       port: 22
       script: |
         rm -rf ~/public_html/*

    - name: Upload and extract ZIP to public_html
      uses: appleboy/ssh-action@master
      with:
        host: 's3cura.ai'
        username: 'iqfu27x9dob7'
        password: 'Gaurav123#'
        port: 22
        script: |
          # Base64 encode the file for transfer
          base64 out.zip > out.zip.b64
          cat out.zip.b64 | ssh iqfu27x9dob7@s3cura.ai "
            mkdir -p ~/public_html
            cd ~/public_html
            base64 --decode > out.zip
            unzip -o out.zip
            rm out.zip
          "